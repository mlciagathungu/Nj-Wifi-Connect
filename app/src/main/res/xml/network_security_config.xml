<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) traffic for local development and ngrok tunnels -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <!-- Optional: Add your PC's IP address if testing on a real device -->
        <!-- <domain includeSubdomains="true">192.168.1.123</domain> -->
        <!-- Add ngrok domain for HTTP (only if using ngrok http, not https) -->
        <domain includeSubdomains="true">7cb7-102-140-248-44.ngrok-free.app</domain>
    </domain-config>
</network-security-config>